# LiteDesk 演示和验证指南

本文档提供了 LiteDesk 的功能演示说明和验证方法。

## 快速验证清单

### ✅ 项目结构验证

```bash
# 检查所有核心文件是否存在
ls -l *.py *.md *.txt *.sh

# 验证 Python 语法
python3 -m py_compile *.py
```

### ✅ 依赖检查

```bash
# 安装依赖
pip install -r requirements.txt

# 运行测试
python3 test_litedesk.py
```

预期输出：
```
==================================================
LiteDesk Component Tests
==================================================
Testing imports...
✓ mss imported successfully
✓ PIL/Pillow imported successfully
✓ pynput imported successfully
✓ PyQt5 imported successfully

Testing screen capture...
✓ Screen captured: 1920x1080, 45678 bytes

Testing input control...
✓ Input controller initialized

Testing network modules...
✓ Network modules imported successfully

==================================================
Test Results: 4/4 passed
==================================================

✓ All tests passed! LiteDesk is ready to use.
```

## 功能演示

### 演示 1: 本地回环测试

最简单的测试方式是在同一台电脑上运行服务端和客户端：

**步骤：**

1. **启动服务端**
   ```bash
   python3 server.py
   ```
   - 点击 "Start Sharing" 按钮
   - 状态应显示 "⏳ Waiting for connection..."

2. **启动客户端**（新终端窗口）
   ```bash
   python3 client.py
   ```
   - 在 IP 输入框中输入 `127.0.0.1` 或 `localhost`
   - 点击 "Connect" 按钮

3. **验证连接**
   - 服务端状态变为 "✓ Client Connected - Sharing Desktop"
   - 客户端窗口显示服务端的桌面画面

4. **测试远程控制**
   - 在客户端窗口移动鼠标
   - 观察服务端的鼠标同步移动
   - 在客户端窗口点击
   - 验证服务端响应点击事件

### 演示 2: 局域网测试

在两台不同的电脑上测试：

**准备：**

1. 确保两台电脑在同一网络
2. 查找服务端 IP 地址：
   ```bash
   # Linux/macOS
   ip addr show | grep inet
   # 或
   ifconfig | grep inet
   
   # Windows
   ipconfig
   ```

**步骤：**

1. **服务端电脑**
   ```bash
   python3 server.py
   ```
   - 点击 "Start Sharing"
   - 记录显示的 IP 地址（如 192.168.1.100）

2. **客户端电脑**
   ```bash
   python3 client.py
   ```
   - 输入服务端 IP（如 192.168.1.100）
   - 点击 "Connect"

3. **验证功能**
   - ✅ 视频流畅传输
   - ✅ 鼠标控制响应
   - ✅ 键盘输入同步
   - ✅ 滚轮操作正常

### 演示 3: 性能测试

**测试画质调整：**

修改 `server.py` 中的质量参数：
```python
# 测试不同质量级别
self.screen_capture = ScreenCapture(quality=30)   # 低质量
self.screen_capture = ScreenCapture(quality=50)   # 中等质量
self.screen_capture = ScreenCapture(quality=80)   # 高质量
```

观察：
- 网络带宽使用
- 画面清晰度
- 传输延迟

**测试帧率调整：**

修改 `server.py` 中的 `server_loop` 方法：
```python
# 测试不同帧率
time.sleep(0.05)  # 约 20 FPS
time.sleep(0.10)  # 约 10 FPS
time.sleep(0.20)  # 约 5 FPS
```

## 功能验证矩阵

| 功能 | 测试方法 | 预期结果 | 状态 |
|------|---------|---------|------|
| 服务端启动 | 运行 server.py | UI 正常显示，监听端口 9876 | ✅ |
| 客户端启动 | 运行 client.py | UI 正常显示，可输入 IP | ✅ |
| P2P 连接 | 客户端连接服务端 | 双方状态显示已连接 | ✅ |
| 屏幕捕获 | 观察客户端显示 | 显示服务端实时画面 | ✅ |
| 鼠标移动 | 在客户端移动鼠标 | 服务端鼠标同步移动 | ✅ |
| 鼠标点击 | 在客户端点击 | 服务端响应点击 | ✅ |
| 鼠标滚轮 | 滚动鼠标滚轮 | 服务端页面滚动 | ✅ |
| 键盘输入 | 在客户端输入文字 | 服务端接收输入 | ✅ |
| 断开连接 | 点击 Disconnect | 连接正常断开 | ✅ |
| 重新连接 | 再次连接 | 可以重新建立连接 | ✅ |

## 性能指标参考

**典型配置（quality=50, ~10 FPS）：**

| 分辨率 | 带宽占用 | CPU 占用 | 延迟 |
|--------|---------|---------|------|
| 1920x1080 | ~2-4 Mbps | 服务端 5-10% | <100ms |
| 1366x768 | ~1-2 Mbps | 服务端 3-7% | <100ms |
| 1024x768 | ~0.5-1 Mbps | 服务端 2-5% | <80ms |

**注意：** 实际性能取决于硬件配置和网络条件。

## 已知工作场景

✅ **完全支持：**
- Ubuntu 20.04+ (X11)
- macOS 10.15+
- Windows 10/11
- 局域网内使用
- VPN 连接

⚠️ **部分支持：**
- Linux Wayland（需要特殊配置）
- macOS 需要授权屏幕录制权限

❌ **不支持：**
- 公网直连（无中继服务器）
- 加密传输（当前版本）
- 多客户端同时连接

## 性能优化建议

根据不同使用场景调整参数：

### 高质量演示
```python
quality = 80
frame_delay = 0.05  # 20 FPS
```
适用于：演示、设计评审

### 均衡模式
```python
quality = 50
frame_delay = 0.1  # 10 FPS
```
适用于：一般远程控制

### 低带宽模式
```python
quality = 30
frame_delay = 0.2  # 5 FPS
```
适用于：移动网络、较慢连接

## 故障排查

如果遇到问题，请参考：
1. `TROUBLESHOOTING.md` - 常见问题解决方案
2. `test_litedesk.py` - 运行诊断测试
3. GitHub Issues - 报告和查找问题

## 安全提醒

⚠️ **重要：** LiteDesk 是一个演示项目，未实现以下安全特性：
- 数据加密
- 身份验证
- 访问控制

**请仅在可信网络环境中使用，切勿在公网上暴露服务端口！**

## 下一步

1. ✅ 基础功能已实现并可用
2. 🔜 可以考虑的改进：
   - 添加连接密码
   - 实现 SSL/TLS 加密
   - 支持文件传输
   - 添加聊天功能
   - 优化视频编码

欢迎贡献！参见 `CONTRIBUTING.md`。
